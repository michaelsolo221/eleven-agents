[project]
name = "eleven-agents-server"
version = "0.1.0"
description = "ElevenLabs post-call webhook receiver and HMAC verification"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.140.0",
    "uvicorn[standard]>=0.51.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]
